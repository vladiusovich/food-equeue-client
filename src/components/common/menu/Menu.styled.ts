import styled from "styled-components";

const S = {
    container: styled.div`
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        width: 100%;
    `,
};

export default S;
