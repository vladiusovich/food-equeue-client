/* eslint-disable max-len */
import styled from "styled-components";

const S = {
    container: styled.div`
        position: fixed;
        bottom: 0;
        width: 100%;
    `,
};

export default S;
